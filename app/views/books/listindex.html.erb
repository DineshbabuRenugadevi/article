<div id="page-content">
    <div class="row justify-content-end mr-1" ,data-toggle="tooltip" data-placement="left" title="Click on Table View" >
        <%= link_to "List View", books_path, class: "btn btn-outline-primary" %>
    </div>
    
    <h1 class="text-center mt-3">Listing of Books</h1>
        <div class="flickr_pagination">
            <%= will_paginate @books, :container => false %>
        </div>
    <%= render 'book' %>
    <div class="flickr_pagination mb-4">
            <%= will_paginate @books, :container => false %>
    </div>
</div>
